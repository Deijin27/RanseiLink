<UserControl x:Class="RanseiLink.Windows.Views.AnimationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:vm="clr-namespace:RanseiLink.GuiCore.ViewModels;assembly=RanseiLink.GuiCore"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             d:DataContext="{d:DesignInstance vm:AnimationViewModel, IsDesignTimeCreatable=False}"
             mc:Ignorable="d" HorizontalAlignment="Stretch">
    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
        <Button Command="{Binding ExportCommand}" Margin="0,0,5,0">Export</Button>
        <Button Command="{Binding ImportCommand}" Margin="0,0,5,0">Import</Button>
        <Button Command="{Binding RevertCommand}" Margin="0,0,5,0">Revert to default</Button>
        <TextBlock VerticalAlignment="Center" Text="{Binding IsOverriden, StringFormat='Is Overriden: {0}'}" d:Text="Is Overriden: true"/>
    </StackPanel>
</UserControl>
